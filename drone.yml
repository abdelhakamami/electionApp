kind: pipeline
name: build 
type: kubernetes

steps:
- name: docker
  image: plugins/docker
  settings:
    registry: docker.io/abdelhakavaxia
    username: docker_username
    password: docker_password
    dockerfile: ./back-end/Dockerfile
    repo: abdelhakamami/election-app-backend
    context: ./back-end
    tags: dev
   
